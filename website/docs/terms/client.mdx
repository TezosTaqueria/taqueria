---
id: client  
title: Client
hoverText: An application that interacts with Tezos nodes using the RPC interface
---

### Term explanation

Client is the general term for a Tezos application that interacts with the nodes. It may generate and manage keys; create and submit operations to the nodes; and query data from the nodes. Clients communicate with nodes via the node's RPC interface.