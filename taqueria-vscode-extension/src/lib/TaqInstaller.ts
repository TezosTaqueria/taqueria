import { sleep } from '../test/suite/utils/utils';

/**
 * @returns true if installed successfully, false is not installed
 */
export default async function downloadAndInstallTaqCLI(): Promise<boolean> {
	// TODO: heavy machinery to implement here
	await sleep(3000);

	return false;
}
