{ parameter (or (address %makeACall) (lambda %updateFeedbackFunction address string)) ;
  storage (pair (lambda %feedbackFunction address string) (address %someAddress)) ;
  code { UNPAIR ;
         SWAP ;
         UNPAIR ;
         DIG 2 ;
         IF_LEFT { DIG 2 ; DROP } { SWAP ; DROP ; SWAP } ;
         SWAP ;
         PAIR ;
         NIL operation ;
         PAIR } }

